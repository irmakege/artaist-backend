
## 💁‍♀️ How to use

- Create an environment: `python3.9 -m venv env`
- Activate the environment: `source env/bin/activate`
- Install required dependencies with `pip install -r requirements.txt`
- Start server with `uvicorn app:app --reload`